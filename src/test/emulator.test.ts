import * as assert from 'assert';
import { getEmulatorLaunchCommand } from "../utilities/index";

suite("Emulator", function(): void {
    // Defines a Mocha unit test
    test("Should Create Proper Emulator URI", function(): void {
        // const uri = getEmulatorLaunchCommand('http://localhost:3978');
        // assert.equal(`${ uri.scheme }://${ uri.authority }?${ uri.query }`, `bfemulator://livechat.open?botUrl=http://localhost:3978`);
    });

});